#include <stdio.h>
#include <string.h>

int main(){

    int a = 10;
    int b = 20;
    float flt = 1.0;
    char string[] = "Hello World";

    printf("There is an integer %d, a second integer %d, a float %f and a string %s", a, b, flt, string);

    return 0;
}